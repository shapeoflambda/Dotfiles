snippet '(\b\w+\b)\.iter' "iterate over a slice" r
for _, ${1:`!p snip.rv = match.group(1)[:-1] if match.group(1).endswith('s') else 'item'`} := range `!p snip.rv = match.group(1)` {
	${0}
}
endsnippet
